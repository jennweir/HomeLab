# Hashicorp Terraform

## Usage

### 1. Log in to HCP Terraform via the CLI

Run `terraform login` and retrieve API token.

### 2. Apply IaC changes

`terraform init`
`terraform plan`
`terraform apply`
